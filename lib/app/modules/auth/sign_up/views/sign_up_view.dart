import 'package:flutter/material.dart';

import 'package:get/get.dart';

class SignUpView extends GetView {
  @override
  Widget build(BuildContext context) {
    return Card(
      child: Text(
        'Sign Up View is working',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
