import 'package:flutter/material.dart';

class Cols extends StatelessWidget {
  final String title;
  final String widthFactor;
  final String name;

  const Cols({Key key, this.title, this.widthFactor, this.name}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
